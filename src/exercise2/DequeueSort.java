package exercise2;

import section2.Sort;

public class DequeueSort {

    /***
     *
     *  mark first element as head
     *  while elemSwapped
     *   while(headNotReached)
     *      compare first two elements
     *          if elem 1 > elem 2
     *              swap elem 1 & elem 2
     *              push elem 1 to bottom of the deck
     *          else
     *              push elem 1 to bottom of the deck
     *      if(head is at second position)
     *          then headReached
     *          push first element to end of the list
     *      if(noSwaps)
     *          then elements not swapped (This means that list is sorted)
     *
     *
     ***/

}
